# CLAUDE.md

このファイルは ExecDB プロジェクトで作業する際、Claude Code が常に踏まえるべき
前提を示す。

## ExecDBとは

DBエンジンとデータ領域を1つの実行ファイル内に保持する、環境構築不要の
ポータブルな単一バイナリRDBMS。Go製、内部エンジンは `modernc.org/sqlite`、
外部I/FはPostgreSQLワイヤープロトコル互換。詳細は `execdb_spec.md` を参照。

## 参照すべきファイル

作業を始める前に、以下を確認すること。

- **`execdb_spec.md`** — ExecDBの仕様（現状ドラフト版）。コアコンセプト、
  アクセス制御、REPLコマンド体系、保存・永続化仕様、ユースケース、ライブラリ
  アーキテクチャ、技術選定、外部I/Fプロトコル仕様、起動オプション、ライフ
  サイクル、GitHubリポジトリ情報までを網羅する（テスト方針・開発フェーズ・
  配布方法は `.claude/rules/` 側の役目であり、ここには含まれない）。
  **実装しながら育てていくドキュメントであり、実装と仕様がズレた場合は
  このファイルを更新すること。**
- **`PLAN.md`** — 実装計画・進捗管理（開発フェーズ①〜④、現在地、保留事項）。
  `execdb_spec.md` と同じくリポジトリ直下に置く一級ドキュメント。**セッション
  をまたぐたびに「現在地」を確認・更新すること。**
- **`.claude/rules/naming.md`** — 命名規則（REPLコマンド・CLIオプション・
  `engine` API名の対応関係、短縮フラグ、タイムスタンプ生成ルール）
- **`.claude/rules/cli-output.md`** — CLI出力（言語、ログ出力先、保存確認
  プロンプトの扱い、サーバーモードの停止・保存）
- **`.claude/rules/pgwire.md`** — PostgreSQL互換ワイヤープロトコル（採用範囲、
  型マッピング、認証、トランスポート）
- **`.claude/rules/testing.md`** — テスト方針（開発環境の前提、実装後の
  動作確認について）
- **`.claude/rules/directory-structure.md`** — ディレクトリ構成（`engine`と
  `cmd/execdb`の役割分担、配置の判断基準）
- **`.claude/rules/distribution.md`** — 配布方法（GitHub Releases、
  `go install`、リポジトリへのバイナリ非コミット方針）
- **`.claude/rules/binary-size.md`** — 実行ファイルサイズ（積極的な最適化は
  不要だが、むやみに肥大化する実装は避ける）
- **`.claude/rules/sqlite-quirks.md`** — SQLite/`modernc.org/sqlite`特有の
  落とし穴（`Deserialize`の反映範囲等、仕様書だけでは判断できない実装上の
  注意点）

これらのルールファイルは、実装中に得た細かい気づき・教訓を Claude Code 自身が
育てていくものである。新しく気づいたルール・過去に踏んだ落とし穴などがあれば、
該当するファイルに追記すること。どのファイルにも当てはまらない新しい種類の
気づきであれば、新しいルールファイルを作ってよい。

## 開発の進め方

- 現在の開発フェーズは `PLAN.md` の「現在地」を確認すること。
  フェーズが進んだら、このファイルの「現在地」を更新すること。
- 実装後は `.claude/rules/testing.md` に従い、ビルド確認（`make build` 等）に
  加えて実際の動作確認（`make test` 等）を行うこと。判断に迷ったら実行する
  側に倒すこと。
- 依存ライブラリを追加する場合、ライセンスがMIT（本プロジェクトのライセンス）
  と衝突しないか確認すること（GPL系等は避ける。詳細は `.claude/rules/testing.md`
  の trivy チェックの節を参照）。

## 権限・自動化について

`.claude/settings.json` により、`git push`/`pull`/`fetch`/`clone`、
`curl`/`wget`、パッケージインストール系コマンド（`*install*`, `go get`）は
実行前に確認を求める設定になっている。確認を求められた場合、無理に実行しよう
とせず、指示を仰ぐこと。

ビルドの自動フック（`go build`/`make build` 等）は現時点では設定していない
（`Makefile` の中身が固まっていないため。詳細は `PLAN.md`
の「保留事項」参照）。実装がある程度進んだら、フック設定の要否を提案すること。

## ルール・スキルの提案

作業を進める中で、以下に気づいたら、都度こちらから提案すること（提案するだけで、
勝手に作成・適用はしない。判断はこちらが行う）。

- **新しいルールにした方がよさそうな知見**: 同じ種類の判断や落とし穴に複数回
  遭遇した、既存の `.claude/rules/` のどれにも当てはまらない、といった場合。
  どのファイルに追記すべきか、あるいは新規ファイルが必要かも合わせて提案する。
- **Skill化した方が効率的そうな作業**: 「同じ手順を3回目繰り返している」など、
  パターン化できそうな一連の作業に気づいた場合。まだ実装が薄い段階では
  無理に提案する必要はない。パターンが実際に繰り返されてから提案すること。

提案は気づいたタイミングで随時行ってよく、まとめて報告するために貯めておく
必要はない。
